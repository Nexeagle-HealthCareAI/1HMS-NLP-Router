# Dependencies for running the test suite (tests/). Not needed by the
# deployed API image -- installed in CI and by developers locally, on top
# of requirements.txt (and requirements-voice.txt if you're also touching
# voice/).
pytest==7.4.3
httpx==0.28.1
# tests/test_voice.py tests voice/api_client.py's HTTP-client logic (mocked
# requests calls) unconditionally -- unlike SpeechRecognition/PyAudio/
# indic-transliteration (genuinely optional, needs system audio libraries),
# `requests` is a hard, lightweight, pure-Python dependency of api_client.py
# with no reason to skip testing it just because the audio stack isn't
# installed. Keep this pin in sync with requirements-voice.txt.
requests==2.32.3

[pytest]
testpaths = tests
markers =
    integration: exercises the real trained bundle/dataset on disk (symptom_specialist_classifier.joblib, Hinglish_Symptoms_V28.csv) instead of a small in-memory fixture. Slower; skipped automatically if those files aren't present. Run explicitly with: pytest -m integration

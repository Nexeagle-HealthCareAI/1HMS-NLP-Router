# ═══════════════════════════════════════════════════════════════════════════════
# EASYHMS NLP SYMPTOM ROUTER — CI/CD Pipeline  (FastAPI/scikit-learn -> GHCR -> VM)
# ═══════════════════════════════════════════════════════════════════════════════
#
# STAGES:
#   Stage 1 — Build        pip install + import smoke test
#   Stage 2 — Docker       build & push image to GHCR
#   Stage 3 — Deploy Dev   pull image -> run on Dev VM  (develop branch, port 5003)
#   Stage 4 — Deploy Prod  pull image -> run on Prod VM (main branch,    port 5003)
#
# No secrets are baked into the image or required by the app itself (no DB, no API
# keys) — it just needs the SAME SSH secrets already used by the other EasyHMS
# backend deploy workflows (this repo has its own copies, since GitHub secrets
# aren't shared across repos):
#   GHCR_TOKEN            GitHub PAT (write:packages + repo)
#   DEV_SSH_HOST / DEV_SSH_USER / DEV_SSH_PASSWORD    Dev VM (151.185.45.77)
#   PROD_SSH_HOST / PROD_SSH_USER / PROD_SSH_KEY_B64  Prod App VM (151.185.45.67)
# ═══════════════════════════════════════════════════════════════════════════════

name: EasyHMS NLP Router — CI/CD

on:
  push:
    branches: [develop, main]
    paths-ignore:
      - "location_API/**"
      - "tests/location_API/**"
      - ".github/workflows/deploy-location-api.yml"
  pull_request:
    branches: [develop, main]
    paths-ignore:
      - "location_API/**"
      - "tests/location_API/**"
      - ".github/workflows/deploy-location-api.yml"
  workflow_dispatch:

concurrency:
  group: easyhms-nlp-router-${{ github.ref }}
  cancel-in-progress: false

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/easyhms-nlp-router

jobs:
# ═══════════════════════════════════════════════════════════════════════════════
  build:
    name: "Stage 1 — Build"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - run: pip install -r requirements.txt -r requirements-dev.txt

      - name: Import smoke test
        run: python -c "import api.main"

      - name: Run test suite
        run: python -m pytest tests/ -v --ignore=tests/location_API

# ═══════════════════════════════════════════════════════════════════════════════
  docker:
    name: "Stage 2 — Docker Build & Push"
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name != 'pull_request'
    outputs:
      image_tag: ${{ steps.meta.outputs.version }}

    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=,suffix=,format=short
            type=ref,event=branch
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

# ═══════════════════════════════════════════════════════════════════════════════
  deploy-dev:
    name: "Stage 3 — Deploy → Dev"
    runs-on: ubuntu-latest
    needs: docker
    if: github.ref == 'refs/heads/develop'
    environment: development

    steps:
      - name: Deploy to Dev VM
        uses: appleboy/ssh-action@v1.0.3
        env:
          IMAGE: ${{ env.IMAGE_NAME }}:${{ needs.docker.outputs.image_tag }}
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
          ACTOR: ${{ github.actor }}
        with:
          host: ${{ secrets.DEV_SSH_HOST }}
          username: ${{ secrets.DEV_SSH_USER }}
          password: ${{ secrets.DEV_SSH_PASSWORD }}
          port: 22
          command_timeout: 10m
          envs: IMAGE,GHCR_TOKEN,ACTOR
          script: |
            set -e
            IMAGE_LC=$(printf '%s' "$IMAGE" | tr '[:upper:]' '[:lower:]')
            echo "$GHCR_TOKEN" | docker login ghcr.io -u "$ACTOR" --password-stdin

            PULLED=0
            for i in 1 2 3; do
              if docker pull "$IMAGE_LC"; then PULLED=1; break; fi
              echo "docker pull attempt $i failed, retrying in 5s..."
              sleep 5
            done
            if [ "$PULLED" != 1 ]; then
              echo "docker pull failed after 3 attempts — aborting without touching the running container" >&2
              exit 1
            fi

            docker rm -f nlp-router-dev 2>/dev/null || true
            docker run -d --name nlp-router-dev --restart unless-stopped \
              --network host "$IMAGE_LC"
            docker image prune -f || true

      - name: Health check
        run: |
          sleep 10
          for i in 1 2 3 4 5; do
            S=$(curl -s -o /dev/null -w '%{http_code}' --max-time 5 \
                http://${{ secrets.DEV_SSH_HOST }}:5003/health 2>/dev/null || echo 000)
            echo "Attempt $i — HTTP $S"
            [ "$S" = "200" ] && echo "✅ NLP Router (dev) is up" && exit 0
            sleep 10
          done
          echo "⚠️ Health check inconclusive — check: docker logs nlp-router-dev"
          exit 0

# ═══════════════════════════════════════════════════════════════════════════════
  deploy-prod:
    name: "Stage 4 — Deploy → Prod"
    runs-on: ubuntu-latest
    needs: docker
    if: github.ref == 'refs/heads/main'
    environment: production

    steps:
      - name: Prepare SSH key
        run: |
          echo "${{ secrets.PROD_SSH_KEY_B64 }}" | base64 -d > /tmp/deploy_key
          chmod 600 /tmp/deploy_key

      - name: Pull & restart container
        env:
          IMAGE: "${{ env.IMAGE_NAME }}:${{ needs.docker.outputs.image_tag }}"
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
          ACTOR: ${{ github.actor }}
        run: |
          IMAGE_LC=$(printf '%s' "$IMAGE" | tr '[:upper:]' '[:lower:]')
          ssh -i /tmp/deploy_key -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null \
              ${{ secrets.PROD_SSH_USER }}@${{ secrets.PROD_SSH_HOST }} \
              "set -e; \
               echo '$GHCR_TOKEN' | docker login ghcr.io -u '$ACTOR' --password-stdin; \
               PULLED=0; \
               for i in 1 2 3; do \
                 if docker pull '$IMAGE_LC'; then PULLED=1; break; fi; \
                 sleep 5; \
               done; \
               if [ \"\$PULLED\" != 1 ]; then echo 'docker pull failed after 3 attempts, aborting without touching the running container' >&2; exit 1; fi; \
               docker rm -f nlp-router-prod 2>/dev/null || true; \
               docker run -d --name nlp-router-prod --restart unless-stopped \
                 --network host '$IMAGE_LC'; \
               docker image prune -f"

      - name: Health check
        run: |
          sleep 15
          for i in 1 2 3 4 5; do
            S=$(curl -s -o /dev/null -w '%{http_code}' --max-time 5 \
                http://${{ secrets.PROD_SSH_HOST }}:5003/health 2>/dev/null || echo 000)
            echo "Attempt $i — HTTP $S"
            [ "$S" = "200" ] && echo "✅ NLP Router (prod) is up" && exit 0
            sleep 10
          done
          echo "⚠️ Health check inconclusive — check: docker logs nlp-router-prod"
          exit 0

      - name: Write summary
        if: always()
        run: |
          echo "## 🧠 EasyHMS NLP Router — Prod" >> $GITHUB_STEP_SUMMARY
          [ "${{ job.status }}" = "success" ] \
            && echo "**Status:** ✅ Deployed" >> $GITHUB_STEP_SUMMARY \
            || echo "**Status:** ❌ Failed" >> $GITHUB_STEP_SUMMARY
          echo "| Image tag | \`${{ needs.docker.outputs.image_tag }}\` |" >> $GITHUB_STEP_SUMMARY
